<!-- Comments only, deliberately: a heartbeat file with content in it schedules
     API calls, and this deployment answers a heartbeat with HEARTBEAT_OK and
     nothing else. Shipped so the slot is ours rather than a generic default
     seeded by the gateway -- a workspace file cannot be left absent, only
     filled by somebody. -->
